# Main .env for Prisma migrations and potentially shared Supabase config if needed at root
# Used by `npx prisma migrate dev` and `npx prisma migrate deploy`

# Connection string for your PostgreSQL database
# Replace with your actual database connection string
# Example for Supabase: postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL="postgresql://user:password@host:port/database?schema=public"

# Supabase project URL and Anon Key (if needed by any root-level scripts, though typically frontends handle their own)
# VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
# VITE_SUPABASE_ANON_KEY="your-supabase-anon-key"

# For mobile app (place this in mobile_app/.env)
# EXPO_PUBLIC_SUPABASE_URL="https://your-project-ref.supabase.co"
# EXPO_PUBLIC_SUPABASE_ANON_KEY="your-supabase-anon-key"

# For admin dashboard (place this in admin-dahsboard/.env)
# VITE_SUPABASE_URL="https://your-project-ref.supabase.co"
# VITE_SUPABASE_ANON_KEY="your-supabase-anon-key"
